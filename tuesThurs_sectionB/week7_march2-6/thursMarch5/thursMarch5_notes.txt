Project 6 due Friday
- stats file

number of games played
number of games won

don't assume stats file exists
    File f = new File(name of stats file);
    if (f.exists()) {
        //means that the stats file DOES exists
        //so we can read from it
    }
    else {
        //never played before
        //start stats at 0 games, 0 wins
    }



Exam 2 is next week (Thursday, March 12)

/////////////////////////

Today:
-Breaking project into methods

--> look for code duplication
--> identify chunks of code related to a single job, break them into
    a different method

/////////////////////////

Hangman example

/////////////////////////

Lab 12
- global Scanner for user input
- createBoard: create array with '_'

    public static char[][] createBoard()

- printBoard: prints board to the screen

    public static void printBoard(char[][] board)

- isFull: returns whether board is full

    public static boolean isFull(char[][] borad)

- getRow: takes turn (char), gets and returns user row

    public static int getRow(char turn)

- getCol: takes turn (char), gets and returns user column

    public static int getRow(char turn)

- isWinner: takes board and turn, returns if player is winner

    public static boolean winner(char[][] board, char turn)