Calling parent versions of methods

///////////////////

Project 9 due Friday

RealNumber, RationalNumber, IntegerNumber

What types are these?

real: anything that can be represented on a number line 
    3.24, 1/2, 8

rational: anything that can be repesented as a fraction
    number that is real but not rational: pi 
    ALL rational numbers are also real

integers: whole numbers
    number that is rational but an integer: 1/2
    ALL integers are also rational (use 1 as denom)

RealNumber
RationalNumber extends RealNumber
IntegerNumber extends RationalNumber

///

Creating real numbers:

RealNumber one = new RealNumber(3.2);
RealNumber two = new RealNumber(4.5);
//how to add one and two?

RealNumber answer = one.plus(two);


///

public class RealNumber {
    //field?
    protected double num;

    //constructor?
    public RealNumber(double n) {
        num = n;
    }

    //plus method?
    public RealNumber plus(RealNumber another) {
        return new RealNumber(num + another.num)
    }

    //others are similar
}



//start of RationalNumber
public class RationalNumber extends RealNumber {
    //fields?
    protected int num;
    protected int denom;

    //still need the constructor
    public RationalNumber(int n, int d) {
        super(n/(double)d);
        num = n;
        denom = d;
    }

    //what should the operations look like?
    public RealNumber plus(RealNumber another) {
        //we know the calling object is a rational

        //am I trying to add another rational?
        if (another instanceof RationalNumber) {
            RationalNumber second = (RationalNumber)another;

            //now find the new numerator and new denom of your result
            //create and return a new RationalNumber with that info
        }
        else {
            //trying to add it to a real
            return super.plus(another);
        }
    }
}



//start of IntegerNumber
public class IntegerNumber extends RationalNumber {
    //field?
    private int value;

    //still need the constructor
    public IntegerNumber(int x) {
        super(x, 1);

        value = x;
    }

    //what should the operations look like?
    public RealNumber plus(RealNumber another) 

        //are you trying to add another integer?
            //the result should be an integer
        //else, call super
}


////

What to do after completing the number classes?

hardcode creating different kinds of numbers
try different operations


///

Next:

Input is like: 
2/6+ 3/4
0.57 -4



What to look for first? operations (+, -, *)

How to get the two numbers on either side?
    split or substring

String piece = "   0.57 ";
String trimmed = piece.trim(); //"0.57"




Then how to tell what kind of number?

real number? 
    Double.parseDouble
    try/catch

rational number? 
    look for /
    either side is integers

no slash or decimal? 
    integer
    Integer.parseInt
    use try/catch


How to tell if invalid?

0.57 + apple



///////////////////

enums

public enum DayOfWeek {
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday,
    Sunday
}

DayOfWeek day = DayOfWeek.Monday;


////////////////////

Look at lab 19 start.

What information is common?

What operations are common?

What to do with unit cost?